features:
  prefix: "feature"
  max_features: 200        # 400 = trop large
  min_corr_abs: 0.02       # supprime les features inutiles
  max_na_ratio: 0.10       # supprime les features trop manquantes
  standardize: true        # obligatoire pour MLP / LGBM régulier
